{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core permissions for the main window. The dashboard sidecar is spawned from Rust, so no shell JS permissions are required.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
