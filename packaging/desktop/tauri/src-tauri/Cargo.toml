[package]
name = "hermes-desktop-app"
version = "0.1.0"
description = "Hermes Agent desktop shell"
authors = ["Nous Research"]
edition = "2021"
rust-version = "1.77.2"

[lib]
name = "hermes_desktop_app_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
